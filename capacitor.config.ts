import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test_tecnico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
